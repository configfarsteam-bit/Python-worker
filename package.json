{
  "scripts": {
    "deploy": "uv add -r requirements.txt --no-sync && cp requirements.txt /tmp/requirements.txt && rm requirements.txt && uv run pywrangler deploy; code=$?; cp /tmp/requirements.txt requirements.txt; exit $code"
  }
}
